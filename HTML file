<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Dopaminergic Dynamics</title>
</head>

<body>

  <h1>Dopaminergic Pathways of the Drugs used to Treat Parkinson's Disease</h1>

  <p>We dived into WikiData and made a query to see what drugs are used to treat Parkinson's disease,
  while filtering out drugs that affect the Dopamine receptors.</p>

  <p>Here is what our SPARQL query looks like:</p>
  <img src="query.jpg" alt="SPARQL query for WikiData" width="900" height="300">

<script>
  const sparqlEndpoint = "https://sparql.wikipathways.org/sparql";
  const sparqlQuery = `
###### Put all SPARQL queries here

### Selects all Parkinson's drugs that interact with at least one of the five dopamine receptors
SELECT ?item ?itemLabel ?filterLabel ?roleLabel ?role
WHERE {
  VALUES ?filter {
  wd:Q21110867
  wd:Q2034004
  wd:Q21110872
  wd:Q308856
  wd:Q21131500}.
  FILTER EXISTS {?item wdt:P129 ?filter.}.
  ?item wdt:P2175 ?role.
  FILTER(?role = wd:Q11085).

  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],mul,en".}
  }

  `;

  </script>
  </body>
  </html>
